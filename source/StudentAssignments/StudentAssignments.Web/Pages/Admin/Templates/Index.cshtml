@page
@model StudentAssignments.Web.Pages.Admin.Templates.IndexModel

@{
    ViewData["Title"] = "Шаблоны";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-page="Create">Добавить шаблон</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Template[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Template) {
        <tr>
            <td>
                <a asp-page="./Details" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                <a asp-page="./Delete" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
